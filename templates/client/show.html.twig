{% extends 'base.html.twig' %} {% block title %}Afficher client -
LePtitJardinier{% endblock %} {% block content %}
<div class="client">
  <h1>Détails de {{ client.nom }} {{ client.prenom }} :</h1>

  <div class="container float-start">
    <table class="table">
      <tbody>
        <tr>
          <th>Id :</th>
          <td>{{ client.id }}</td>
        </tr>
        <tr>
          <th>Nom :</th>
          <td>{{ client.nom }}</td>
        </tr>
        <tr>
          <th>Prenom :</th>
          <td>{{ client.prenom }}</td>
        </tr>
        <tr>
          <th>Adresse :</th>
          <td>{{ client.adresse }}</td>
        </tr>
        <tr>
          <th>Ville :</th>
          <td>{{ client.ville }}</td>
        </tr>
        <tr>
          <th>Cp :</th>
          <td>{{ client.cp }}</td>
        </tr>
        <tr>
          <th>Type client</th>
          <td>{{ client.typeClient }}</td>
        </tr>
      </tbody>
    </table>
    <a href="{{ path('client_index') }}" class="afficher-client"
      >Afficher les clients</a
    >

    <a
      href="{{ path('client_edit', { id: client.id }) }}"
      class="afficher-client"
      >Modifier</a
    >

    {{ include("client/_delete_form.html.twig") }}
  </div>
</div>
{% endblock %}
